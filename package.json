{
  "name": "wardrobe",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.3.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev --filter=@wardrobe/web",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "typecheck": "turbo typecheck",
    "vercel-build": "turbo build --filter=@wardrobe/web"
  },
  "devDependencies": {
    "turbo": "^2.8.0",
    "typescript": "^5"
  }
}
