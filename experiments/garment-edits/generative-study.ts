import { resolve } from "node:path";
import sharp from "sharp";
import prompts from "./generative-prompts.json";

const root = resolve(import.meta.dir, "../../output/cutout-prototype/generative-v1");
const cases = [
  { name: "Polo · arm and phone occlusion", note: "The arm and phone are gone and the shirt is complete. Fabric, emblem and hidden construction have been synthesized; this is a plausible catalog representation, not a pixel-preserving extraction." },
  { name: "Trousers · missing waistband and legs", note: "A full pair is reconstructed, but the source cannot confirm its waistband, pockets, lower-leg fit or length. Browser inspection confirms a largely clean transparent exterior. A second edit targeting edge treatment changed fabric detail despite instructions to preserve it." },
  { name: "Watch · tiny side-on source", note: "Skin is removed, but the model invents detailed case and strap geometry from a 72×50 crop. The result cannot establish the identity of the actual watch. A clearer source is needed." },
];
const escape = (s: string) => s.replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;");
const evidence = [];
for (const [i, item] of cases.entries()) {
  const image = sharp(resolve(root, `${i}-generated.png`));
  const metadata = await image.metadata();
  const stats = await image.stats();
  evidence.push({ item: item.name, file: `${i}-generated.png`, width: metadata.width, height: metadata.height, hasAlpha: metadata.hasAlpha, alpha: stats.channels[3], assessment: item.note });
}
await Bun.write(resolve(root, "result.private.json"), JSON.stringify({ mode: prompts.mode, model: prompts.model, scope: "One original photo; three items; visual evaluation only. No API integration or deployment.", evidence }, null, 2));
const html = `<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Wardrobe · Generative edit study</title>
<style>*{box-sizing:border-box}body{margin:0;background:#f3f0e8;color:#242b28;font:16px/1.55 system-ui}main{max-width:1080px;margin:auto;padding:32px 20px}h1{font-size:clamp(28px,5vw,46px);line-height:1.1}h2{font-size:22px}.intro{max-width:790px}.badge{font-size:12px;letter-spacing:.12em;text-transform:uppercase;color:#536254}nav{position:sticky;top:0;background:#f3f0e8ed;padding:14px 0;z-index:1;display:flex;gap:8px;align-items:center;flex-wrap:wrap}button,a{color:inherit}button{cursor:pointer;border:1px solid #8a978b;border-radius:20px;padding:8px 15px;background:white}button[aria-pressed=true]{background:#263a2e;color:white}article{padding:22px 0 30px;border-top:1px solid #c7cfc5}.pair{display:grid;grid-template-columns:1fr 1fr;gap:16px}figure{margin:0;min-width:0}figcaption{margin:8px 0;font-size:14px}.stage{height:460px;padding:14px;border:1px solid #d1d6ce;border-radius:12px;background-color:white;display:flex;align-items:center;justify-content:center}.stage img{max-width:100%;max-height:100%;object-fit:contain}body[data-bg=dark] .stage{background:#202522}body[data-bg=check] .stage{background-color:#fff;background-image:conic-gradient(#ddd 25%,transparent 0 50%,#ddd 0 75%,transparent 0);background-size:24px 24px}.assessment{max-width:850px}summary{cursor:pointer}pre{white-space:pre-wrap;overflow-wrap:anywhere;font:13px/1.6 system-ui}details{margin-top:16px}.notice{padding:14px 18px;border-left:3px solid #987b44;background:#ebe5d5}@media(max-width:560px){main{padding:24px 12px}.pair{gap:8px}.stage{height:300px;padding:6px}h2{font-size:19px}}</style>
<body data-bg="light"><main><div class="badge">Wardrobe / local evaluation</div><h1>From worn photo to catalog image</h1><p class="intro">Three generative image edits from the same source photo. Each request used the verified crop and full photograph for context. The segmentation approach is parked; this study tests reconstruction and identity preservation.</p><p class="notice">Generated previews contain inferred detail. Original photos remain authoritative. Nothing here is deployed or saved into your wardrobe.</p><nav aria-label="Preview background"><span>Inspect edges:</span><button data-bg="light" aria-pressed="true">Light</button><button data-bg="dark" aria-pressed="false">Dark</button><button data-bg="check" aria-pressed="false">Checkerboard</button></nav>
${cases.map((item, i) => `<article><h2>${escape(item.name)}</h2><div class="pair"><figure><div class="stage"><img src="${i}-before.jpg" alt="Original crop for ${escape(item.name)}"></div><figcaption>Original crop</figcaption></figure><figure><div class="stage"><img src="${i}-generated.png" alt="Generative edit for ${escape(item.name)}"></div><figcaption>Generated preview · <a href="${i}-generated.png" download>Download PNG</a></figcaption></figure></div><p class="assessment">${escape(item.note)}</p><details><summary>Exact edit prompt</summary><pre>${escape(prompts.prompts[i]!)}</pre></details>${i===1?'<details><summary>Second attempt: edge-only refinement</summary><div class="stage"><img src="1-refinement.png" alt="Second pants edit with changed fabric details"></div><p>Not selected: the cloth changed despite an edge-only edit request.</p></details>':''}</article>`).join("")}
<p>Built-in image generation tool. Exact backend model version was not exposed. Nano Banana was not tested. All three initial outputs have real alpha channels; that alone does not establish clean edges or correct garment identity.</p></main><script>for(const button of document.querySelectorAll('button[data-bg]'))button.addEventListener('click',()=>{document.body.dataset.bg=button.dataset.bg;for(const other of document.querySelectorAll('button[data-bg]'))other.setAttribute('aria-pressed',String(other===button));});</script></body></html>`;
await Bun.write(resolve(root, "index.html"), html);
console.log(`Wrote local gallery and evidence to ${root}`);
